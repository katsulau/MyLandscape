.posts-index-wrapper
  .contents
    .posts.clearfix
      - @posts.each do |post|
        = link_to "/posts/#{post.id}", data: {"turbolinks"=>false} do
          = image_tag post.image.to_s, class: "post"
    = paginate @posts
